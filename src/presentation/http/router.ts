// Placeholder: legacy or aggregate HTTP router entry point.
// Current application routing starts in app.ts and routes/v1/index.ts.
// Implement this only if routing is centralized outside versioned routers.
