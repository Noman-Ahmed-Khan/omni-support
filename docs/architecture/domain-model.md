# Domain Model

Placeholder for the OmniSupport domain model.

Current domain behavior lives in the entity, value object, repository interface, and event files under `src/domain`. Fill this document when the aggregate boundaries and lifecycle rules are stable enough to document for contributors.
